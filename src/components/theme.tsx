import { createTheme } from "@suid/material";

export const THEME = createTheme({
    palette: {
        mode: "dark",
        primary: {
            main: "#fb0202",
        },
        secondary: {
            main: "#00ffff",
        },
    },
});